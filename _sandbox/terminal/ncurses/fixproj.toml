[general]
name = "sandbox-ncurses"
version = "0.1.0"

[build]
files = [
    "ncurses.fix",
]
opt_level = "separated"
objects = ["lib.o"]
preliminary_commands = [["make", "lib.o"]]
dynamic_links = ["ncursesw"]

