module Main;

type [m: *->*] ReaderT e m a = struct {};

type Reader e a = struct {};

type Obj e a = struct {
    //data: Reader e a      // こちらだと問題はおきない
    data: ReaderT e IO a    // こちらだと以下のメッセージが出てパニックする
            // thread 'main' panicked at src/optimization/unwrap_newtype.rs:393:39:
            // called `Option::unwrap()` on a `None` value
};

main: IO () = pure();
