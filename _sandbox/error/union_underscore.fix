module Main;

type Foo = union {
    lowercase: (),
    _UPPERCASE: ()
};

main: IO ();
main = (
    let a: Foo = lowercase();
    let a: Foo = _UPPERCASE();       // no problem
    let a: Foo = Foo::lowercase();
    let a: Foo = Foo::_UPPERCASE();  // `Expected expr_unary.` error
    pure()
);
