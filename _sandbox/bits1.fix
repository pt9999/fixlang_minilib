module Main;

main: IO ();
main = (
    loop_m(
        0, |i|
        if i > 64 { break_m $ () };
        let u64 = -1.to_U64;
        let u64 = u64.shift_right(i.to_U64);
        println("(u64,i)="+(u64,i).to_string);;
        continue_m $ i + 1
    )
);
