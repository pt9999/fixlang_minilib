module Minilib.Monad.Identity;

type Identity a = unbox struct {
    data: a
};

make: a -> Identity a;
make = |a| Identity { data: a };

impl Identity: Monad {
    pure = make;
    bind = |ma, f| make(f(ma.@data));
}
