module Main;

import AsyncTask;

main: IO ();
main = (
    do {
        let _ = *(lift $ eprintln ("started"));
        let str = "abc";
        let buf: Array U8 = Array::fill(100, 0_U8);    
        let res = str.borrow_c_str(|p_str|
            buf.borrow_ptr(|p_ret|
                0_I32
            )
        );
        pure()
    }
    .try(|err| eprintln (err))
);
