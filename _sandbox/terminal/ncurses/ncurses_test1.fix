module Main;

import Minilib.Terminal.Ncurses;

main: IO ();
main = do {
    Ncurses::run $ |ncurses| (
        let win = *ncurses.get_default_window;
        win.clear;;
        win.move_add_str(10, 10, "Hello world");;
        let c = *win.getch;
        pure()
    )
}.try(eprintln);
