module Main;

type Wrap a = unbox struct { data: a };

func: I64 -> a -> a = |level, a| (
    if level <= 0 { a };
    func2(level - 1, a)
);

func2: I64 -> a -> a = |level, a| (
    func(level, Wrap { data: a }).@data
);

main: IO () = (
    println(func(100, "test"))
);
