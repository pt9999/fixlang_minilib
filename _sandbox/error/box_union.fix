module Main;
import HashMap;

type Node = box union {
    branch: HashMap String Node,
    leaf: ()
};

type Obj = box struct {
    map: HashMap String Obj
};

main: IO ();
main = (
    let _ = leaf();
    //let _ = Obj { map: HashMap::empty(0) };
    pure()
);
